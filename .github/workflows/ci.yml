name: CI

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  backend-build:
    runs-on: ubuntu-latest
    steps:
      - name: CI disabled by org policy
        run: |
          echo "Team-9-kursus policy blocks external actions (checkout/setup-java)."
          echo "Run mvn locally or use course-provided actions/self-hosted runner."
